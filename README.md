*The end is nigh!*

## The Rule I implemented

"f" or "F" is replaced by "ph"
Lowercase "l" but not Uppercase "L" is replaced by "Llll"

## Expects I used

expect().toBe();
expect().toContain();
expect().not.toBe();
